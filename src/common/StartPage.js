import React from "react";
import styles from "./StartPage.module.css";

const StartPage = () => {
  return (
    <div className={styles.container}>
      <p className={styles.note}>Choose a type of muscle to find out more</p>
    </div>
  );
};

export default StartPage;
